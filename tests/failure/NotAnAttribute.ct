// <Plain(1)> references a contract that does not inherit from Attribute,
// so it is not an attribute type.
Contract Plain {
    static fn Main() {
        IO.Println("x");
    }
}

<Plain(1)>
Contract Other {
    static fn Main() {
        IO.Println("y");
    }
}
