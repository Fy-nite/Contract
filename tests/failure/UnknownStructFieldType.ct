import __builtin.std;

// Unknown field type on a struct (top-level and nested).
struct BadStruct {
    y: NoSuchType;
}

Contract Program {
    struct Inner {
        z: Missing;
    }

    static fn Main() {
        IO.Println("x");
    }
}
