import __builtin.std;

// Attributes cannot be applied to fields yet (the wire format has no field
// attribute slot).
Contract Program {
    <Deprecated("why")>
    x: int;

    static fn Main() {
        IO.Println("x");
    }
}
