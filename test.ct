Contract Test {
    fn main() {
        var x: int = 5;
        switch (x) {
            case 1: IO.Println("one");
            case 2: IO.Println("two");
            case 5: IO.Println("five");
            else: IO.Println("other");
        }
    }
}