Contract Test {
    fn main() {
        var x = 5;
        switch (x) {
            case 1: print("one");
            case 2: print("two");
            case 5: print("five");
            else: print("other");
        }
    }
}